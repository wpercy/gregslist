<% provide(:title, 'All users') %>
<div id="articles" data-articles="<%= render(template: "users/index.json.rabl") %>">
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>
